<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bulbulhossen.gifimageview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AndroidPageCurlActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    <activity android:name="ListExample"
        android:screenOrientation="portrait"
        android:label="@string/list_example">
    </activity>

    <activity android:screenOrientation="portrait"
        android:label="@string/standalone_example"
        android:name="StandaloneExample">
    </activity>

    </application>

</manifest>
